<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- الإنترنت مطلوب للاتصال بالـ Middleware -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- تحديد موقع التوصيل (اختياريّ) عند الدفع -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- صلاحيات Tabby (التحقق من الهوية / KYC) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Tint Store"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              الروابط العميقة (المهمّة 3.3): tint://category/<slug>

              ‼️ مخطَّطٌ خاصّ لا App Links على الدومين — عمداً في هذه المرحلة.
              App Links تتطلّب ملفّ `assetlinks.json` على الدومين وبصمة توقيع
              الإصدار، وبدونهما يفتح النظام الرابط في المتصفّح بلا رسالة خطأ.
              والمخطّط الخاصّ يعمل فوراً وبلا اعتماد على الاستضافة.

              ‼️ و`autoVerify` غير مضبوط لأنّه لا معنى له مع مخطَّطٍ خاصّ —
              وضبطُه يُظهر تحذيراً في Play Console عند الرفع.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tint" />
            </intent-filter>
        </activity>
        <!--
          ‼️ **أيقونة الإشعار الافتراضيّة ولونها.**

          بلا هذين كان يُستعمل رمز التطبيق كما هو، فيرسمه النظام **دائرةً
          بيضاء فارغة** بلا علامة — وهو ما ظهر في كلّ إشعاراتنا التجريبيّة.

          والأيقونة ظِلٌّ أبيض على شفّاف عمداً: أندرويد يقرأ قناة الشفافيّة
          وحدها ويمسح الألوان، فأيقونةٌ ملوّنة تظهر مربّعاً أبيض صلداً.

          واللون يُلوّن الأيقونة واسم التطبيق — لا نصّ الإشعار.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_tint" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/tint_notification" />
        <!--
          ‼️ **القناة الافتراضيّة — وإلّا صنع Firebase قناةً باسم
          «Miscellaneous».**

          كُشفت على المحاكي (2026-08-12): إشعارٌ يصل بلا `channelId` يجعل
          Firebase يُنشئ قناةً افتراضيّة **باسمٍ إنجليزيٍّ غامض** تظهر في شاشة
          إعدادات العميل بجوار قنواتنا العربيّة — وهو العيب نفسه الذي رُصد في
          «نون» (`Zoho SalesIQ`) و«شي إن» (`latest offers` مكرّرة).

          والخادم يُصرّح بالقناة في كلّ حملة؛ وهذا حزامُ أمانٍ ثانٍ لما يُرسَل
          من مكانٍ آخر أو من نسخةٍ أقدم.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="tint_campaigns" />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 11+: للسماح بفتح الروابط الخارجية (Tabby checkout) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
